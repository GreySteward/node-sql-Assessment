/**
 * Created by chrisgibson on 2/19/16.
 */

// err in terminal on all tests. failed at node_asssessment@1.O.0 start script 'node app.js'
//see line 6 package.json

//could not create auto_incrementing primary key in PG following w3 examples. inquire re: proper syntax format

//check PG query formation line 26 routes.js

//unable to test database connectivity

//need to update click function. event listeners/event handlers

// see PG database animal_tracker: 1 table Animals, still needs
// auto_incrementing primary key

// resolved node start issue- duplicate ";" inside "" (YES!!!)

// apply div wrappers to index.html to assign placement to label/input

//browser could not connect to the server on local host: 3000 find out why?